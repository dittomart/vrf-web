/** The splash's three-dot loader, reused as the lazy-route fallback. */
export function DotLoader() {
  return (
    <div className="flex items-center justify-center py-20">
      <div className="dot3">
        <span />
        <span />
        <span />
      </div>
    </div>
  );
}
